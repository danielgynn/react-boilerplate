import React, { Component } from "react";
import { Box, Header } from "archetype-ui";

class Profile extends Component {
  render() {
    return (
      <Box padding={[1, 1, 1, 1]}>
        <Header>Profile</Header>
      </Box>
    );
  }
}

export default Profile;
